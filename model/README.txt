PLACE YOUR MODEL HERE
--------------------
Put your Keras model file named `model.h5` inside this folder:
`model/model.h5`

Note: Large model files may make deployment slow. For production, consider TF SavedModel, TF Lite, or hosting the model externally.
